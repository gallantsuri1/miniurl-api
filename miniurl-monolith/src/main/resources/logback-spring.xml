<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- Include Spring Boot defaults -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- Application name -->
    <springProperty scope="context" name="APP_NAME" source="app.name" defaultValue="MyURL API"/>

    <!-- Log file configuration -->
    <property name="LOG_DIR" value="${LOG_DIR:-./logs}"/>
    <property name="LOG_FILE" value="${LOG_FILE:-miniurl}"/>
    <property name="MAX_FILE_SIZE" value="${MAX_FILE_SIZE:-10MB}"/>
    <property name="MAX_HISTORY" value="${MAX_HISTORY:-30}"/>
    <property name="TOTAL_SIZE_CAP" value="${TOTAL_SIZE_CAP:-1GB}"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) %magenta([%thread]) %cyan(${APP_NAME}) : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender with Rolling Policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>
        <prudent>true</prudent>
        
        <!-- Rolling policy: Time-based and Size-based -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rotation with size-based triggering -->
            <fileNamePattern>${LOG_DIR}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            
            <!-- Max file size before rotation -->
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            
            <!-- Keep logs for this many days -->
            <maxHistory>${MAX_HISTORY}</maxHistory>
            
            <!-- Total size cap for all log files -->
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
            
            <!-- Clean up old logs on startup -->
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] ${APP_NAME} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application Loggers -->
    <logger name="com.miniurl" level="DEBUG"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.thymeleaf" level="INFO"/>
    <logger name="org.springframework.mail" level="DEBUG"/>

    <!-- Suppress harmless Chrome DevTools errors -->
    <logger name="org.springframework.web.servlet.resource" level="ERROR"/>

    <!-- Suppress mail health check warnings -->
    <logger name="org.springframework.boot.actuate.mail.MailHealthIndicator" level="ERROR"/>
    <logger name="org.springframework.boot.actuate.mail" level="ERROR"/>
    <logger name="org.eclipse.angus.mail" level="ERROR"/>
    <logger name="jakarta.mail" level="ERROR"/>

    <!-- Hibernate SQL logging (uncomment for debugging) -->
    <!-- <logger name="org.hibernate.SQL" level="DEBUG"/> -->
    <!-- <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/> -->

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
